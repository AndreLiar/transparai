// Global test setup — runs after the test framework is installed
// Add jest matchers, mocks, or global hooks here as needed.

// Increase timeout for integration tests that may hit a real DB
jest.setTimeout(30000);
